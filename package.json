{
  "name": "rebuildr",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "cd rebuildr-backend && npm run lint && cd ../rebuildr-app && npm run lint"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
