## Installation

Install dependencies

```bash
npm install
```

Generate GraphQL types. These will differ in git each time you run it even if you haven't done any changes. Commit them anyways.

```bash
npm run codegen
```

## Run

App only runs on web

```bash
npm run web
```
